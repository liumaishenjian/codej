# S15 Session-local Task List 证据包

## 元数据

```text
Stage: S15 Independent Innovation（Task List capability slice）
Status: Accepted（Capability）；S15 Stage Exit 仍为 Open
Release / Commit: 41c8064e05267ba312238d2f0837402a77fa3bab（第四次 corrective implementation）
Reference Behavior Baseline: R2026.03
Authorized Snapshot ID: AUTH-SRC-2026-07-29-A
Feature IDs: TASK-01、TASK-02、TASK-03、TASK-04、TASK-05
Current → Exit Target Levels: L0 → L2
Owner: codej maintainers
Date: 2026-08-25；updated 2026-08-26
```

## G0：来源与授权

- 依据 `R2026.03-authorized-source.md` 第 24 节，对授权快照中的任务状态、依赖、认领、恢复和终端展示做仓库外只读研究。
- 结论区分 `Observed / Inferred / Unknown`；未复制函数体、Prompt、注释、错误文案、私有名称、文件布局、常量或 Fixture。
- 参考材料只提供可独立表达的行为输入，项目契约、Java/TypeScript 命名、schema、journal 与 UI 均为独立设计。

结论：Accepted。

## G1：范围与可证伪目标

- 目标是状态/持久化/权限独立于 Plan 审批工件的 Session-local Task Board；批准后的唯一显式步骤 section 可单向冻结为权威 Task，但不能反向审批 Plan或替代 Evidence。
- L2 验收覆盖三态与派生 blocked/recovery、依赖 DAG、CAS/claim epoch、四个统一 Pipeline Tool、Resume/Fork、root/child scope、stable snapshot、stdio `/tasks` 与 Ink 面板。
- 关键反证包括：并发 claim 只能一次成功、旧 epoch/越权/环依赖必须拒绝、terminated Run 不自动重放、日志随 mutation 近线性增长、Plugin/MCP spoof 不能获得 Session state 权限。
- Team Board、跨进程订阅、peer messaging、lease 与自动领取延期 `SUB-11`。

结论：Accepted。

## G2：研究与 ADR

- ADR-088 冻结 root-owned Board、单向 canonical `blockedBy`、派生 `blocks/blocked/recoveryRequired`、revision/tombstone、claim 与幂等契约。
- 四个 Tool 采用独立 snake_case schema，只能经过唯一 `ToolExecutionPipeline`；Permission 仅放行精确 BUILT_IN name/effect 组合。
- Session JSONL 保存增量 mutation delta；Resume 重建，Fork 写单次完整 seed；稳定协议只读，不开放 mutation 旁路。
- Ink 只消费事件和快照，不取得 Task 执行权，也不修改 canonical final。

结论：Accepted。

## G3：独立实现

- Domain/Core 不依赖 Spring、文件系统、JSON、stdio 或 Ink；Session journal、stable/stdin 和 TUI 均位于架构边缘。
- `task_create/task_update/task_list/task_get` 已注册为生产 BUILT_IN Tool，并复用 Permission、Approval、Lifecycle、Hook、截断与结构化错误主链。
- root 可向 `delegate_agent.taskIds` 提出候选 ID，宿主重新验证并注入最多 32 项的 child capability；嵌套 child 不继承 parent Board。
- 新增核心协议、安全边界、状态机和公共契约均使用中文 Javadoc；JSON null 通过 `JsonNull` 保持字段缺失与显式删除的差异。

结论：Accepted。

## G4：验证

### Java 全量回归

```text
Command: .\mvnw.cmd clean verify
Environment: Windows 11、Java 21、Maven Wrapper 3.9.16
Date: 2026-08-25
Commit: 0a7a4a1149371ab0e52c68838cf453cf37d2a8f1
Passed / Failed / Skipped: 1274 / 0 / 13；199 份 Surefire XML；11 个 reactor 模块 SUCCESS
Metric Before: TASK-01..05 L0，生产链不可用
Metric After: TASK-01..05 L2，Domain/Core/CLI/Session/Protocol/SDK 全量回归通过
```

专项测试包含 Task Domain 9、Core 状态机/Tool/Pipeline、生产 composition 3、Session journal 5、stable protocol、stdio command、child composition，以及 JsonNull 的 Domain/Provider/Session 边界。

该 clean verify 在 commit 的隔离本地 clone 中运行，因为维护者当前正在运行的 `codej --stdio` 进程合法持有主工作区 jar；验证目录仅包含 commit `0a7a4a1`，完成统计后已删除。未关闭或干扰该用户进程。

### Ink/TUI 全量回归

```text
Command: npm --prefix cc-java-tui run check
Environment: Windows 11、Node.js 22
Date: 2026-08-25
Commit: 0a7a4a1149371ab0e52c68838cf453cf37d2a8f1
Passed / Failed / Skipped: build PASS；17 files / 293 tests PASS；真实 Java stdio→Ink 4/4 PASS
Metric Before: 只有 `/tasks` 查询面板和分层测试，mutation 不会自动刷新 Ink；批准 Plan 的真实执行 scope 还会对 Task Tool 返回 `unknown_tool`
Metric After: 普通复杂任务与批准 Plan 均实时投影 PENDING/IN_PROGRESS/COMPLETED；自动面板不抢 Steering；完成态直接断言 strikethrough/dim
```

### Corrective 真实交互证据

首次执行 `npm --prefix cc-java-tui run test:real-java` 时，Plan 场景产生三个 `unknown_tool`（`task_create`、两次 `task_update`），从而证伪“提示模型使用 Task Tool 即等于交互完成”。原因是批准 Plan 的独立 execution configuration 未重新包含 open 后注入的 Task Tool。修复后同一命令 4/4 PASS，并验证：

- 成功 Task mutation 的事件严格为 `tool.completed → task.board.snapshot`，失败 mutation 不推送；
- 普通复杂任务和批准 Plan 均出现 revision 1/2/3 与 PENDING/IN_PROGRESS/COMPLETED；
- 自动面板不聚焦，运行中 Steering 仍提交；手动 `/tasks` 才接管方向键；
- 真实 E2E 看到完成行，组件策略直接断言 `strikethrough=true`、`dimColor=true`。

### 第二次 corrective：批准步骤、紧凑交互与终态收敛

用户真实运行进一步证伪旧实现：批准 Markdown 的中文步骤未成为 Task，模型重新生成英文标题；全宽边框泄漏
Task ID/revision/“Java”；全部 Task 与证据完成后仍继续请求模型直至 `time_limit_reached`。Commit
`dd1e8885555f0b538c535f63148068e9694f121d` 修复并验证：

- 唯一 `拟定步骤/实施步骤/执行步骤`（及受控英文等价）下的顶层有序项按原语言、顺序、正文预置；18 项城市编号、代码 fence、嵌套 heading 不进入 Board，零 section 保持 legacy，多 section Fail Closed；
- seed metadata 绑定 Plan identity/digest/稳定的用户批准 revision，mutation actorRunId 是 Runtime 真实执行 Run；初始化失败以配对 `INTERNAL_ERROR` 生命周期收口；
- 批准 execution 不再向模型暴露 `task_create`，初始 PENDING snapshot 在 `run.started` 后、首个 Tool 前发布；
- Task 与确定性 Evidence 全部满足后只允许一次零 Tool final-only；违规 Tool Call 在执行前拒绝，最终 Run `completed` 且 durable Plan 发布 `plan.verification.completed`；
- Ink 改为紧凑无边框列表，隐藏 ID/revision/owner/实现词，进行中加粗、完成项删除线/dim；全部完成约 5 秒后只隐藏并可 `/tasks` 重开；
- 20/24/240 列真实 AgentView 渲染覆盖 CJK、emoji、combining、依赖、恢复和控制行，相关整行 display width 不超过 columns。

验证记录：

```text
Focused Java: AgentRuntimeTest 41/41；ApprovedPlanTaskSeederTest 7/7；
              TaskToolProductionCompositionTest 7/7；RuntimeStdioCommandHandlerTest 48/48
TUI check: build PASS；17 files / 297 tests PASS
Real Java stdio→Ink: 4/4 PASS
Clean verify: isolated clone at dd1e888；200 reports；1287 tests / 0 failures / 0 errors / 13 skipped；11 modules SUCCESS
```

主工作区首次 `clean verify` 在 clean 阶段因现有 codej 进程锁定 `cc-java-domain-0.1.1.jar` 而未进入测试，未关闭用户进程；
隔离 clone 首次全量运行真实发现 `StdioProtocolFixtureCleanupTest` 仍期待旧 `task_create` 序列，修正为应用 seed + 四个执行 Tool 后专项 3/3，通过后第二次完整 clean verify 成功。两次失败均如实保留，不能算作通过。

### 第三、四次 corrective：真实 XLSX、窄状态协议与 Run deadline

用户再次真实运行证伪了三点：早期 `.xlsx` E2E 只是同扩展名文本；strict stdio codec 未接受 dispatcher
已支持的 `/tasks`；批准 execution 暴露通用 operation/revision/claim schema，使模型只读取列表而不逐项更新。
同一执行还证明默认 5m 的绝对 Run deadline 会在首次命令失败、修补后取消第二次命令。

实现 Commit `41c8064e05267ba312238d2f0837402a77fa3bab` 完成：

- 批准 Plan 的 `task_update` 只公开 `task_id/status/active_form`，Java 注入 Plan identity、CAS revision、
  claim epoch、Session/Run 和 actor capability，并只允许 PENDING→IN_PROGRESS→COMPLETED；
- Provider call ID 与 active/status 阶段通过 SHA-256 派生固定短 Task call ID；128 个非 BMP code point、
  partial retry、重复调用不漂移 revision，越权 Plan 与非法跃迁 Fail Closed；
- Ink 进行中主行使用实心符号/bold，下一行显示 dim activity 与有界省略号；完成主行真实
  strikethrough/dim 且移除 activity。React renderer 直接断言节点属性，不以纯文本替代样式证据；
- 默认 Run timeout 从 5m 调整到既有安全上限 30m，显式 `--timeout` 仍硬覆盖，最大值未扩大；
- 独立 JDK 子进程通过 `run_command` 生成 OpenXML ZIP，重新读取并验证 18 个城市×7天=126 行；
  五项中文任务均观察到 PENDING/IN_PROGRESS/COMPLETED。另一真实命令 timeout 保持
  `IN_PROGRESS + recoveryRequired`，Run terminal 计数、`/tasks` 和 Ink 提示一致。

最终验证记录：

```text
Focused Java: ApprovedPlanTaskUpdateToolTest 2/2；production/CLI/codec 35/35；
              StdioProtocolFixtureCleanupTest + related 12/12
TUI check: build PASS；17 files / 298 tests PASS
Real Java stdio→Ink→run_command: 6/6 PASS；真实 XLSX 与 timeout recovery 均通过
Clean verify: 203 reports；1305 tests / 0 failures / 0 errors / 34 skipped；11 modules SUCCESS
```

完整验证失败历史同样保留：第一次在 CLI 544 项中暴露旧 snapshot revision 期望 `[1,2,3]`，
窄 Adapter 一次 Tool 内先写 activeForm 再 claim，正确权威 revision 是 `[1,3,4]`；专项修正后通过。
第二次在 `cc-java-model-spring-ai` 启动 Surefire fork 时进程异常退出且 0 测试执行；第三次完整
`clean verify` 取得上述全绿结果。两次失败均未被计为通过。

### 容量与安全负例

- 256 个各含 4096-byte description 的 Task journal 小于 4 MiB，并可 Resume 为 256 项。
- 环依赖、缺失/tombstoned 依赖、旧 revision/epoch、blocked claim/complete、child 越权和跨 Board capability 均 Fail Closed。
- PLAN、显式 Deny、Plugin/MCP spoof、名称/Effect 错配保持拒绝；精确 BUILT_IN `delegate_agent` 仍需审批。

### Javadoc 审计

```text
Command: .\mvnw.cmd --% -DskipTests -Dmaven.javadoc.failOnWarnings=true javadoc:aggregate
Result: 全局严格聚合仍因本分支既有 Headless Plan/ModelRetry 等公共契约文档欠账失败；不得宣称 0 warning
Task-related warning filter after correction: 0
```

首次严格运行暴露本切片 `TaskMutation`、Task metadata、child scope 与 journal 端口的 record/方法参数说明缺失，已补齐中文 `@param/@return`；最终严格输出中 `domain/task`、`core/task`、`ChildTaskRequest`、`TaskJournalJson` 和 `taskBoardSnapshot` 相关警告为 0。剩余全局欠账不是 Task List 能力证据，也未被本变更伪装为通过。

结论：Accepted。

## G5：可复现 Demo

- Demo：`docs/demos/S15-task-list.md`。
- 正例覆盖模型创建/更新/列表/详情、Resume/Fork、父任务委托 child、stable snapshot 与 Ink 交互。
- 负例覆盖 blocked claim、依赖环、旧 claim epoch、PLAN delegate 与伪造 ToolSource。
- 2026-08-26 实际执行结果：完整 Maven、TUI 与真实 Java 跨进程 XLSX/timeout 回归均通过；真实 Provider 不作为本 L2 capability 的前提。

结论：Accepted。

## G6：退出对账

- `TASK-01..05` 继续维持 L2；第四次 corrective 更新 PRD、技术设计、ADR、授权研究、Demo、Gap Report 与矩阵行为描述，不产生等级提升。
- Progress Dashboard 的代码/矩阵摘要、生成、`--check` 与 `--self-test` 必须在最终提交前通过。
- `SUB-11` 保持 L0；S15 的 PERM-05、双 Provider 和 L4 A/B Eval blocker 未关闭，因此只接受本 capability，不宣称 S15 Stage Exit。

结论：Accepted（Capability）；Stage Exit Open。
