# R2026.03 已授权参考源码受控研究登记

- Snapshot ID: `AUTH-SRC-2026-07-29-A`
- Status: `ACTIVE FOR CONTROLLED STUDY`
- Recorded: 2026-07-29
- Candidate Upstream Reference: `https://github.com/liumaishenjian/claude-code`
- Local Read-only Paths（按设备择一）:
  - `E:\Java\cc-java-reference\claude-code-main`
  - `G:\AI Cloud\claude-code-main`
- Local Bytes ↔ Candidate Upstream Relationship: `Unknown`
- Exact Revision / Tag / Release: `Unknown`
- License / Rights Holder: `Unknown`
- Authorization Evidence: 项目维护者于 2026-07-29 明确确认已经取得该本地材料的学习授权
- Authorized Scope: 仓库外只读研究、机制抽象、独立行为与验证方法提炼
- Not Authorized by This Record: 复制、逐行翻译、派生分发、再发布或把参考字节带入本仓库

## 1. 快照身份

本登记复用 2026-07-28 对同一组本地字节计算的最小身份。它用于确保研究者讨论的是
同一份材料，不证明其与候选上游某个 Commit 完全一致，也不补齐许可证或权利人信息。

| 字段 | 值 |
| --- | --- |
| 文件数 | 1,902 |
| 总字节数 | 30,382,832 |
| Tree SHA-256 | `5f820b7a05b704a5e49cfd7747189af265def28a73227889c3ff028aeab79301` |
| Exact Revision | `Unknown` |

2026-08-10 对当前 `G:\AI Cloud\claude-code-main` 的文件枚举发现，目录还包含 6 个不属于登记 payload 的本机 wrapper 文件：根 `README.md` 与 `.idea/**` 下 5 个文件，合计 3,421 bytes。明确排除这 6 个 wrapper 后，授权 payload 恰为上表登记的 1,902 files / 30,382,832 bytes；本次只复核文件数/字节边界，**没有重新计算或重新宣称验证 Tree SHA-256**，也不改变 `AUTH-SRC-2026-07-29-A` 身份。

本快照此前曾以 `UNVERIFIED-SRC-2026-03-31-A` 隔离。维护者的明确授权确认使其能够在
本项目内恢复为受控学习输入；来源 Revision、许可证和公开再使用权仍然保持 `Unknown`，
因此不能把“允许学习”扩大解释为“允许复制或分发”。

## 2. 允许提炼的内容

- 子系统职责、依赖方向和边界；
- 状态转换、终止条件、协议不变量和失败恢复；
- 算法策略、决策顺序、取消与资源清理方法；
- 可独立表达的公开行为、故障场景和评测方法；
- `Observed / Inferred / Unknown` 形式的最小机制结论。

## 3. 禁止进入本仓库的内容

- 参考函数体、内部 Prompt、注释和错误文案；
- 私有类型名、文件布局、实现常量和内部数据格式；
- 大段研究摘录、参考源码文本或私有数据；
- 参考仓库、依赖、子模块、Fixture、Golden Output 或发布物；
- 不能由本项目 PRD、ADR、独立契约和测试解释的逐行 Java 翻译。

每个要进入产品需求、技术设计或实现的研究结论，必须先由单独 ADR 说明采纳范围、
本项目的独立设计、被否决部分和可证伪验证。仅阅读材料不提升 Capability Level。

## 4. 本次 S02 CLI 机制观察

以下结论只记录抽象机制，不记录参考表达：

| 分类 | 结论 |
| --- | --- |
| Observed | 交互层使用 TypeScript 与 React 式终端组件模型，参数解析、原始输入、布局和差量渲染是独立职责 |
| Observed | TTY 与非 TTY 路径分离；交互输入、取消和最终输出不是同一套展示策略 |
| Observed | 终端 UI 复杂度显著高于简单逐行 REPL，包含输入状态、重绘、宽度和信号处理 |
| Inferred | Java Runtime 与终端 UI 应通过事件/命令边界解耦，避免 UI 框架进入 Domain/Core |
| Unknown | 本地字节对应的准确发行版本、上游 Commit、完整构建方式和跨平台验证结果 |

这些观察只用于支持 [ADR-023](../adr/ADR-023-s02-java-headless-ink-tui.md) 的方案比较；
最终协议、命名、模块和测试均由 `cc-java` 独立定义。

## 5. S03 搜索机制观察

以下结论只记录抽象机制，不记录参考表达：

| 分类 | 结论 |
| --- | --- |
| Observed | 仓库精确内容检索由独立 Tool 驱动 ripgrep，而不是以向量 RAG 替代文件/行号证据 |
| Observed | Tool 参数被转换为结构化进程参数，并覆盖正则、过滤、输出模式、上下文和结果窗口 |
| Observed | 搜索进程具有超时/取消、输出裁剪、ignore/VCS 过滤、相对路径化和资源失败恢复职责 |
| Inferred | Java 重实现应隔离 Search Backend，并用 ProcessBuilder 参数数组与 WorkspaceGuard 保持确定性安全边界 |
| Unknown | 准确发行版本、完整跨平台打包方式以及各策略在公开产品版本中的稳定性 |

这些观察只用于支持
[ADR-033](../adr/ADR-033-s03-ripgrep-search-backend.md)；本项目接口、错误、参数子集和测试
均独立定义。

## 6. S05 Permission Pipeline 机制观察

以下结论来自对权限类型、规则装载、决策器、审批队列、文件与进程权限检查以及统一 Tool
执行入口的受控只读研究；只记录可独立表达的机制，不记录参考函数体、私有类型名、文件
布局、错误文案或实现常量。

| 分类 | 结论 |
| --- | --- |
| Observed | 权限决策把 `allow / ask / deny` 与决定原因、规则来源和可选更新范围分开表达；`ask` 必须由交互或受信控制面收敛 |
| Observed | 规则至少可以按 Tool 及可选内容范围匹配，并保留内存 Session 与磁盘配置等来源；显式 deny 和需强制询问的安全检查早于普通 allow 与模式放行 |
| Observed | 模式与规则不是同一概念；“接受编辑”只为受控编辑路径提供快捷允许，保护性检查仍可要求审批，非交互入口不能展示请求时安全拒绝 |
| Observed | Session approval 通过对当前权限上下文追加有界规则实现，生命周期止于当前进程；批准一次不会自动批准后续调用 |
| Observed | 内置与外部 Tool 在执行层复用同一权限检查和结果归一化入口，来源信息参与诊断或规则匹配但不构成绕过凭据 |
| Observed | 交互审批存在多个可能竞争的决定来源，并使用一次性收敛与取消清理避免过期决定执行 Tool |
| Observed | 重复拒绝会被显式计数并在达到阈值后改变恢复策略，成功执行可打断连续拒绝；具体阈值不作为本项目契约输入 |
| Inferred | Java S05 应把可信 Hard Denial、显式规则、模式默认、Session Grant 与 Approval 组合成一个类型化策略内核，并由 Core/Pipeline 发布权威生命周期 |
| Unknown | 当前快照对应的准确发行版本、规则跨版本兼容、全部 Managed Policy 语义以及各种外部 Surface 的稳定协议 |

本项目的采纳和偏离见
[ADR-038](../adr/ADR-038-s05-authorized-permission-study.md)；独立 S05 契约见
[ADR-039](../adr/ADR-039-s05-permission-pipeline.md)。参考实现不作为测试 Oracle。

## 7. S06 Session + Checkpoint 机制观察

以下结论来自对持久 Transcript、Session 身份/Workspace 归属、恢复/分叉、活跃所有权、
未配对 Tool、文件修改历史与显式恢复的受控只读研究；只记录可独立表达的机制，不记录参考
函数体、私有名称、文件布局、错误文案、常量或内部数据格式。

| 分类 | 结论 |
| --- | --- |
| Observed | 持久对话保存聚合语义记录，而不是把模型流的每个 token 当作规范 Transcript 条目；低价值进度展示与规范历史分离 |
| Observed | Session 身份、Workspace 归属和元数据是独立职责；Resume 复用原 Session，Fork 创建新身份并保留来源关系，后续追加不修改原 Session |
| Observed | 恢复会重建有效规范链并识别缺少对应结果的 Tool 使用；中断不能被解释为可安全再次执行副作用 |
| Observed | 活跃 Session 使用显式所有权/进程登记，并区分仍活跃与陈旧状态 |
| Observed | 文件恢复基于修改前状态或“不存在”标记，独立于 Git；差异检查与恢复由显式用户动作触发 |
| Inferred | Java S06 应把可忽略的观察 sink 与副作用前必须成功的 durable journal 分离，并以 started/completed、write-ahead checkpoint 和 compare-before-restore 建立 Fail Closed 恢复 Gate |
| Unknown | 准确发行版本、内部格式稳定性、跨平台/网络文件系统锁语义、保留/迁移策略和全部副作用恢复覆盖范围 |

采纳边界见
[ADR-040](../adr/ADR-040-s06-authorized-session-checkpoint-study.md)；独立 S06 契约见
[ADR-041](../adr/ADR-041-s06-session-checkpoint.md)。参考内部格式不进入项目，也不作为测试 Oracle。

## 8. S07 Context Engineering + File Memory 机制观察

以下结论来自对请求前 Context 组装、容量减压、Tool 历史清理、滚动/全量摘要、文件记忆索引与
相关召回编排的受控只读研究；只记录可独立表达的职责、状态转换和失败边界。

| 分类 | 结论 |
| --- | --- |
| Observed | 模型请求所见 Context 与持久会话事实分离；压缩服务后续请求，不要求改写规范 Transcript |
| Observed | 大载荷缩减、旧 Tool 输出清理、滚动记忆和全量摘要由不同职责承担，并按压力条件选择；Tool 协议关系保持完整 |
| Observed | 文件记忆分离存储、入口索引、可发现目录、相关召回和请求投影；查询准备可并行，召回失败或迟到不阻断主请求 |
| Inferred | Java S07 应采用 Canonical Transcript/Projection 分离、条件式 C1-C4、M1-M5 文件记忆和 ready-only 零等待消费；失败候选不提交，迟到结果不注入已发送请求 |
| Unknown | 准确发行版本、内部 Prompt/阈值/评分/布局/格式、全平台时序保证、保留迁移与多主机一致性 |

采纳边界见 [ADR-042](../adr/ADR-042-s07-authorized-context-memory-study.md)；独立 Context 与
Memory 契约见 [ADR-043](../adr/ADR-043-s07-context-projection-compaction.md)和
[ADR-044](../adr/ADR-044-s07-file-memory-prefetch.md)。本项目的命名、文件格式和数值上限均为
独立设计，参考实现不作为测试 Oracle。

## 9. S09 Hooks 机制观察

以下结论来自对 Hook 事件元数据、配置快照、匹配、外部 Handler、Compact 与信任路径的受控只读
研究；不记录内部字段、函数、Prompt、文案、布局或常量。

| 分类 | 结论 |
| --- | --- |
| Observed | 事件是否可阻断由生命周期位置决定；完成/观察事件不能撤销已发生副作用 |
| Observed | 配置来源与批准状态先于 Handler 创建；外部进程/HTTP 具有独立 timeout、取消和输出边界 |
| Observed | Compact 前后事件与普通 Tool 事件不同，前置可附加指示或阻断，后置用于观察终态 |
| Inferred | Java S09 应采用独立 Coordinator、稳定有界聚合、线性 glob、精确指纹 Trust 与 transient Context Projection |
| Unknown | 准确发行版本、完整事件集合、内部协议稳定性和全平台进程/网络保证 |

采纳和偏离见 ADR-051/055；本项目 schema、算法、错误与测试均为独立设计。

## 10. S10 MCP 机制观察

以下结论来自对 MCP 配置、连接状态、Transport、Tool/Resource/Prompt、过滤、认证与恢复路径的
受控只读研究，并以公开 MCP Specification/Java SDK 文档交叉验证。

| 分类 | 结论 |
| --- | --- |
| Observed | 每个 Server 保持独立连接状态，多 Server 初始化与失败隔离不应改变稳定配置顺序 |
| Observed | 配置/信任、Transport、协议 Client、Tool Registry 和 Permission 是分离职责 |
| Observed | Tool 过滤/命名空间在发布前完成，断线恢复需要重建 initialize 且必须有界 |
| Observed | Tool、Resource 与 Prompt 是不同 primitive；认证状态与普通 Tool 输出分离 |
| Inferred | Java S10 应将官方 SDK 隔离在 Adapter，并把远端 Tool 映射回唯一 Permission/Approval/Pipeline |
| Unknown | 准确参考发行版本、全部 OAuth/cache 行为、远程策略与跨版本兼容保证 |

采纳边界见 ADR-056，独立实现见 ADR-057；授权快照不作为依赖、Fixture 或测试 Oracle。

## 11. S11 Skills + Plugins 机制观察

以下结论来自对 Skill discovery/metadata、调用/投影、资源/Hook、Session 保留，以及 Plugin
manifest/组件/命名空间/安装快照的受控只读研究，并以官方 OpenAI Codex `rust-v0.147.0`
（Commit `be6e8eac029b183056b7e4402879f15d2c85f61b`）公开源码交叉验证。

| 分类 | 结论 |
| --- | --- |
| Observed | Skill catalog 的轻量 metadata 与按调用加载的正文/资源是分离职责；显式和模型调用最终共享解析/执行边界 |
| Observed | Skill 可携带工具约束与生命周期 Hook；Plugin 可把 Skill、Hook、MCP 等组件映射到命名空间 |
| Observed | 运行 Session 使用内容身份稳定的 Skill/Plugin snapshot；磁盘更新不应悄悄改变当前 Session 语义 |
| Inferred | cc-java 的 `allowed-tools` 必须只收窄真实 Tool set，Plugin Tool Provider 必须由宿主 SPI 映射并进入唯一 Pipeline |
| Inferred | 安装与激活应分离，卸载应先 quiesce 并等待在用 snapshot 释放；fingerprint 不能冒充签名或 Sandbox |
| Unknown | 准确授权快照发行版本、内部选择算法/Prompt、完整市场/签名/迁移和全平台并发卸载保证 |

采纳边界见 ADR-058，独立 Skill/Plugin 契约见 ADR-059/060。数值上限、类型名、schema、
错误与测试均为 cc-java 独立设计；S11 明确不加载任意 JAR。

## 12. S12 Sub-Agent + Worktree 机制观察

以下结论来自对 Agent definition、子运行装配、Context/Tool/Permission 隔离、父子委托、模型/预算覆盖、并发/后台/取消、Sub-Agent Hook 与 Git Worktree 的受控只读研究，并以官方 OpenAI Codex `rust-v0.147.0`（annotated tag object `3ed6f04f6bf8b7c46299d1cb1ff99c74ce21a51d` 指向 Commit `be6e8eac029b183056b7e4402879f15d2c85f61b`）公开源码交叉验证。

| 分类 | 结论 |
| --- | --- |
| Observed | Agent definition 与运行实例分离；子运行复用同类 Agent Loop，但拥有独立 Context、Tool/Permission 状态、模型/限制与取消身份 |
| Observed | 父子关系、任务状态、前台/后台、等待/通知与取消是显式职责；并发需要共享上界，嵌套创建也不能绕过容量 |
| Observed | Sub-Agent start/stop 生命周期可附加 Context 或观察终态，但不能替代 Tool Pipeline；后台资源必须在完成、失败或取消后清理 |
| Observed | Git Worktree 提供工作目录/分支隔离，创建、进入、保留、删除和异常恢复需要独立状态与所有权检查 |
| Inferred | cc-java 应以 `AgentSupervisor`、独立 `ChildRuntimeScope`、父预算 reservation、bounded report、共享 permits 和保守 `WorktreeLease` 重新装配同一 `AgentRuntime` |
| Unknown | 授权快照准确版本、参考调度公平性/预算计费/摘要质量、跨重启后台保证，以及 Windows/Linux Worktree 全部故障原子性 |

采纳边界见 ADR-061，独立范围与架构契约见 ADR-062。S12 已在实现 Commit `cfbe0282b37a93e38256c3d2d6f22ed2207975a5` 上完成 commit-scoped G0-G6 与 Stage Exit，相关 Capability 达到冻结的 L2/L1 目标；Worktree、Permission、Checkpoint 与进程清理仍不构成 S13 OS Sandbox。

## 13. S13 Sandbox + Security 机制观察

以下结论来自对 Sandbox Settings/适配、平台能力检测、文件/网络策略、Command 包装、失败回退与违规诊断的受控只读研究，并以官方 OpenAI Codex `rust-v0.147.0`（annotated tag `3ed6f04f6bf8b7c46299d1cb1ff99c74ce21a51d` 指向 Commit `be6e8eac029b183056b7e4402879f15d2c85f61b`）公开源码交叉验证。

| 分类 | 结论 |
| --- | --- |
| Observed | Sandbox 选择、Settings/Managed 策略、平台执行、违规诊断与 fallback 是分离职责；显式要求隔离却不可用时不能静默忽略 |
| Observed | 文件读写、网络、进程、环境/Secret 是独立策略维度；Permission/Approval 不能替代 OS 强制边界 |
| Observed | Command、Hook、MCP/Plugin/Sub-Agent 等来源最终需要一致的执行后端；来源与 Trust 不能自行扩大 Sandbox policy |
| Inferred | cc-java 应以 `ExecutionBackend`、实际 capability probe、不可变 policy、fail-closed selector 与执行前单次显式 Local fallback 重新装配所有本地进程入口 |
| Unknown | 授权快照准确版本及其外部 sandbox runtime 身份；三平台完整等价性、受限 CI/WSL/容器嵌套、DNS rebinding/IPC 和内核级攻击保证 |

采纳边界见 ADR-063，独立范围与架构契约见 ADR-064。S13 已在实现 Commit `8a75d5f5e977ce4c5fcd19fafb3e5776a5ec2bf3` 上完成 Commit-scoped G0-G6 与 Stage Exit；只提升由 Linux A、Container B、native Windows process/env B 与攻击回归证明的 Capability。Permission、Checkpoint、Worktree、Job cleanup、最小环境与 Local backend 继续明确不等于 Sandbox；JVM 内 HTTP 不受进程 backend 强制。

## 14. S15 Plan UX 机制观察

以下结论来自对授权快照中 `/plan` 命令、进入 Plan 的权限请求和退出 Plan 的计划审批请求进行受控只读研究；只提炼职责和状态，不记录参考函数体、Prompt、文案、私有布局或常量。

| 分类 | 结论 |
| --- | --- |
| Observed | Slash 入口负责切换到只读规划状态；带自然语言参数时同一任务继续进入模型查询，无参数时可用于进入模式或查看已有计划 |
| Observed | 进入规划与批准退出是两个独立状态迁移；前者说明只读探索边界，后者完整展示计划并把用户选择收敛为继续执行或继续规划/拒绝 |
| Observed | 计划审批选项与 Permission 更新相连，但 UI 不直接执行工具；批准后的执行仍回到正常 Runtime 与权限控制面 |
| Inferred | cc-java 应保留自己的 Session-owned Plan、digest/recovery/Pipeline Gate，只把自然语言入口和三分支审批作为 Surface UX；内部摘要不得成为用户输入 |
| Unknown | 授权快照准确发行版本、公开行为稳定性、全部多人协作/编辑器/远程 Plan 语义及其跨版本兼容 |

ADR-074 曾以严格 JSON 与内存步骤 Gate 固定第一版 L1；2026-08-20 又按维护者指定范围核对计划文件、退出 Tool、审批组件、消息投影、Session Restore/Conversation Recovery/Storage 与 REPL Resume/Fork 路径（只提炼职责、状态、边界与失败恢复）。补充观察为：计划文件关联可随 Resume 恢复，缺失文件可由持久会话材料重建，而 Fork 必须创建新的可写计划身份以避免互相覆盖；崩溃恢复重建上下文，不等于自动重放副作用。由此推断 cc-java 应采用 Session-owned、revisioned Markdown artifact、canonical journal authoritative recovery，以及独立的 generation/manifest projection。准确版本、内部格式兼容、多主机及完整远程协作仍为 `Unknown`。采纳和独立契约见 ADR-076；ADR-076 部分取代 ADR-074 主体，但保留其审批前只读、批准后唯一 Runtime/Pipeline 和失败关闭不变量。授权快照不作为 Fixture 或测试 Oracle。


2026-08-21 Batch 2 继续只读核对 PermissionMode/permission setup、Plan attachment、退出 review Tool、AskUserQuestion Tool 与 keyboard picker、REPL 状态及恢复路径。新增 `Observed`：规划是可多轮读取、更新计划文件、结构化提问后再请求 review 的持续状态；问题答案以 Tool call 关联恢复同一循环；计划正文与 review 决定分离。新增 `Inferred`：cc-java 应以 durable revision 作为 review 事实、以 callId 关联 picker，并在 definitions/execute 两处复用 capability policy。`Unknown`：参考产品是否对完整 Tool registry 使用普遍 capability filter。项目采用的外部 Tool 默认不可用 hard boundary 是独立安全强化，不声称为观察到的普遍参考行为。采纳见 ADR-077。

## 15. 停止条件

如果授权范围被撤回、材料身份变化、研究者无法遵守只读边界，或后续拟进行复制、分发、
依赖接入等超出学习范围的行为，必须立即停止使用并通过新 ADR 重新评估。

## 16. S15 Batch 3 durable Plan review 与执行交接机制观察

2026-08-21 对 `AUTH-SRC-2026-07-29-A` 进行新的受控只读研究，范围限定为退出 Plan picker、
permission setup/恢复、keep/clear context、拒绝反馈继续、Session resume/fork/crash 以及实现查询调度。

| 分类 | 结论 |
| --- | --- |
| Observed | 退出规划的批准选项与执行期 permission policy 选择相连，但 UI 不直接执行工具 |
| Observed | keep-context 与 clear-context 是明确分支；clear 仍携带计划引用并安排新的实现查询 |
| Observed | 拒绝/继续规划解除当前等待并回到同一交互循环，反馈与批准决定分离 |
| Observed | Session 恢复可重建计划/对话关联，Fork 需要新计划身份；崩溃恢复不表示自动重放副作用 |
| Inferred | 精确 revision 校验、permission restore、ExecutionBrief 与实现查询入队应由单个服务端协调命令线性化 |
| Unknown | 准确发行版本、内部阈值/格式、完整远程并发与全部 crash interleaving |

采纳边界和本项目独立契约见 ADR-078。没有复制/翻译参考函数体、Prompt、文案、名称、布局、常量、
Fixture 或字节；参考快照不作为依赖、Fixture 或测试 Oracle。


## 17. S15 Batch 4 交互预算、Web/Process 失败与策略变化机制观察

2026-08-21 对 `AUTH-SRC-2026-07-29-A` 进行新的受控只读研究，范围限定为 interactive 主循环的
可选 `maxTurns`、Tool/模型 retry、WebFetch/WebSearch HTTP 失败、Bash 进程退出、失败反馈与策略变化。
只提炼职责、状态、不变量与验证方法；未复制或翻译函数体、Prompt、注释、错误文案、私有名称、
布局、常量、Fixture 或字节。

| 分类 | 结论 |
| --- | --- |
| Observed | 主循环最大回合是可选调用输入；显式提供时形成可见终止，而普通交互不必默认携带很小的强制上限 |
| Observed | 模型/HTTP Adapter retry 与模型下一回合再次提出 Tool 是不同控制层；后者不是前者的重试次数 |
| Observed | Web Fetch 可按状态码与少量受信响应头区分部分阻断；403 的认证、UA/ACL 或普通 forbidden 原因并非总可观察 |
| Observed | Shell Adapter 以进程退出状态判定失败并保留有界输出；HTTP CLI 是否以失败退出由命令选项决定 |
| Inferred（2026-08-26 纠正） | cc-java 应保留调用方显式 hard limits，并把总模型回合、总 Tool 次数与 Run deadline 的 absence 建模为一等状态；普通交互不应派生软预算或 absolute ceiling |
| Inferred | 相同 Tool、规范参数和类型化失败的第二次调用应在 Runtime 要求策略变化；不能从 prose/HTML/stderr 猜 fingerprint |
| Unknown | 参考内部是否存在通用跨 Tool failure fingerprint、准确阈值、全部 Provider 的 403 细分和跨 Session 策略缓存 |

2026-08-26 真实 Provider/approved-plan 流程证伪了旧“16/32 软检查点 + absolute ceiling + 默认总
Run deadline”推断：它会在合法长任务完成前终止。修正后本项目以 optional presence 表达三个调用方
hard limit，普通 Interactive/Plan/approved-plan 均 absent；只保留 `ToolFailureCategory`、Run-owned
fingerprint 与 Web Adapter bounded retry，见 ADR-079。retry 数值、canonical 编码、错误码和测试仍为
本项目独立设计；能力等级不因该研究自动提升。

## 18. S15 Batch 5 Plan 完成证据、TUI、launcher 与 transport 机制观察

2026-08-21 对 `AUTH-SRC-2026-07-29-A` 进行新的受控只读研究，范围限定为 Plan execution
completion/verification、Ink key interaction、launcher/build/version selection 和 transport/crash recovery。
未复制或翻译函数体、Prompt、文案、私有名称、布局、常量、Fixture 或字节。

| 分类 | 结论 |
| --- | --- |
| Observed | Plan 批准允许进入实现，但执行与可选后续验证是不同生命周期；验证触发不等于模型 prose 声称成功 |
| Observed | picker 由键盘选择批准策略和 keep/clear，UI 只收敛决定，不直接执行 Tool |
| Observed | Resume/Fork 恢复 Plan 关联，崩溃恢复不自动重放副作用；transport 具有独立关闭/失败状态 |
| Observed | 版本/构建身份进入 launcher 与发行选择，开发构建和可更新发行构建需要区分 |
| Inferred | codej 应让 Plan 完成依赖批准 brief、workspace revision 与确定性 evidence，而不是普通 Run `COMPLETED` |
| Unknown | 参考默认是否强制验证、是否持久逐项 ledger、全部平台 launcher/transport 保证 |

`PlanEvidenceLedger` 是 codej 的独立增强，不声称为参考类型、内部格式或默认行为。采纳见 ADR-080。

## 19. S15 Batch 6 Plan trusted control plane 纠正观察

2026-08-21 继续窄读 Plan 文件维护、退出规划 Tool、审批 picker、REPL keep/clear 与 Resume/Fork 恢复职责。
只提炼机制，不复制或翻译源码表达。

| 分类 | 结论 |
| --- | --- |
| Observed | 模型维护用户可读计划，退出规划 Tool 从当前 Session 关联读取计划并请求审批；未观察到要求模型手工维护计划文件 CAS 的稳定契约 |
| Observed | UI 只提交批准、继续规划或拒绝决定；权限恢复、状态转换和执行仍由应用控制面拥有 |
| Observed | Resume 复用计划关联，Fork 创建独立可写身份；恢复不等于副作用重放 |
| Inferred | codej 自有 revision/contentDigest CAS 应留在 trusted Tool/store boundary，模型只提交 Markdown 或 review intent |
| Unknown | 参考内部是否存在等价 CAS、多人/远程编辑冲突协议和稳定外部 schema |

registered BUILT_IN locator 校验、requirement replacement、`PLAN_ARTIFACT_CONFLICT/UNAVAILABLE` 与
repeated-failure 例外均为 codej 独立 Evidence Ledger/control-plane 设计，见 ADR-081。

## 20. S15 Batch 7 Plan execution Surface 投影与恢复观察

2026-08-22 继续窄读 Plan 退出 picker、permission restore、keep/clear 实现查询、Plan 恢复与
transport/session 状态职责。只提炼机制，不复制或翻译源码表达。

| 分类 | 结论 |
| --- | --- |
| Observed | picker 先收敛批准/拒绝和执行期权限选择，UI 不直接执行 Tool |
| Observed | keep/clear 都保留批准计划并恢复或安排实现查询；拒绝回到规划/反馈路径 |
| Observed | Resume 恢复 Plan 关联、Fork 创建独立身份，恢复不表示自动重放副作用 |
| Observed | transport/session 活动状态与 Plan/permission UI 状态是分离职责 |
| Inferred | codej Surface 应在签发会产生新 Run 的控制命令时预登记 request correlation，并在提交拒绝时回滚 |
| Unknown | 参考产品是否公开稳定 requestId/runId 协议、全部迟到事件策略与所有 transport/crash interleaving |

requestId/runId reducer、optimistic Run rollback、projection notice 与真实 Ink/installed E2E 均为 codej 独立设计，见 ADR-082。

## 21. S15 Batch 8 TUI 滚动、Tool 活动与模型状态机制观察

2026-08-22 对授权快照的终端滚动、Tool 运行展示、模型状态和 Token Usage 职责做受控窄读，并以
公开 `CODEX-0.147` 的事件分层作补充对照。只提炼机制；未复制或翻译源码表达。

| 分类 | 结论 |
| --- | --- |
| Observed | 普通主屏输出与全屏虚拟列表是两种不同终端策略；全屏路径自行维护 viewport、滚动位置和鼠标事件。 |
| Observed | Tool 进行中/完成状态与动作对象可独立展示，但 UI 不取得 Permission 或执行权。 |
| Observed | Tool 摘要与可切换详情由不同交互层承载；stderr、终态失败和退出事实不能因默认折叠而丢失。 |
| Observed | 模型 lifecycle、Token Usage、上下文占用与可公开 reasoning 类型是不同信号。 |
| Observed | `CODEX-0.147` 区分累计 Usage、上下文窗口状态、reasoning summary 与 raw reasoning visibility。 |
| Inferred | codej 当前主屏 Ink 应把已封存 Run 永久写入终端 scrollback、只保留 live region 动态更新；若未来使用 alternate screen，必须另建虚拟列表和鼠标协议。 |
| Inferred | 已进入 Static 的 Run 不应继续依赖可变展开 state；完成后诊断可由独立 live viewer 读取终态快照，避免重绘历史或展示失效快捷键。 |
| Unknown | 准确发行版本、内部重复压缩/活动折叠阈值、全部终端兼容策略及各 Provider 的公开 reasoning summary 稳定性。 |

活动摘要白名单、相邻等价行压缩、历史 live viewer、`model.turn.*` stdio v0 投影、估算/实测标签和不展示隐藏思维链是 codej 独立设计，见 ADR-083。

## 22. S15 Plan evidence correction 与最终交付边界观察

2026-08-22 对授权快照中的 Plan 批准、执行期权限切换、远程执行轮询、Session 恢复与取消状态做受控窄读。只记录职责、状态迁移、失败边界和验证方法；未复制或翻译函数体、Prompt、文案、私有名称、布局、常量、Fixture 或源码字节。

| 分类 | 结论 |
| --- | --- |
| Observed | Plan 批准是显式用户决定，并与执行期 Permission/Context 选择绑定；批准 UI 不直接执行 Tool，后续副作用仍由正常 Runtime 和权限管线拥有。 |
| Observed | 远程执行的活动、完成、失败与取消使用独立状态；瞬时轮询失败采用有界连续失败治理，迟到轮询结果不能覆盖已经停止或切换后的任务状态。 |
| Observed | Session Resume/Fork 恢复计划与对话关联，但恢复本身不表示重新执行已成功 Tool 或自动重放副作用。 |
| Inferred | evidence 失败后的纠正应保持在同一受预算、取消和 Permission 治理的执行 Runtime 中，且只反馈有界结构化失败，不应启动一个会自动重放旧 Tool 的新执行。 |
| Inferred | Surface 的完成声明必须服从确定性 authority；模型 prose 可以暂存或丢弃，但不能在 evidence terminal 前作为已交付结果发布。 |
| Unknown | 授权快照是否存在逐 requirement evidence ledger、最终 Assistant 写入前的 correction continuation、重复失败指纹或与本项目等价的固定纠正次数。 |

`PlanEvidenceLedger`、`FinalAssistantDecision.CONTINUE/STOP`、`PLAN_VERIFICATION_REQUIRED`、两次独立上限、重复失败指纹、stdio final-text gate 和 `plan.verification.correction` 均为 codej 独立设计，见 ADR-086；不声称为参考内部类型、默认数值或稳定外部协议。2026-08-27 corrective 进一步确认：授权观察只支持 Task List 与 Plan/transport 状态的职责分离；planning prompt 不投影内部 Task ID、Evidence 只报告批准 exact locator、历史 Plan failure 不污染独立 Task Surface，均是本项目由可证伪失败驱动的独立边界修正，不是参考表达复制。

## 23. S15 Tool Schema、参数纠错与连续搜索展示观察

2026-08-23 对授权快照中的 Grep 输入 Schema、Tool 输入解析/值校验、Tool Result 配对与连续
Read/Search 聚合进行受控窄读。只记录职责、状态、不变量和验证方法；未复制或翻译函数体、Prompt、
注释、错误文案、私有名称、布局、常量、Fixture 或源码字节。

| 分类 | 结论 |
| --- | --- |
| Observed | Grep 对模型公开严格、单义的输入 Schema，分页使用一个规范字段；类型错误在 Permission 和执行前失败。 |
| Observed | 类型校验失败把具体字段问题返回到原 `tool_use_id`；值校验同样在 Permission 前返回可操作原因。 |
| Observed | 某些 Schema 未投影场景会增加确定性的恢复动作，而非只让模型从泛化异常自行猜测。 |
| Observed | 连续 Read/Search 默认聚合，但错误 Tool Result 仍保持可观察，UI 不取得执行或 Permission 权限。 |
| Inferred | cc-java 应分离 advertised canonical Schema 与 legacy parser，并把 validation correction 作为隐私安全结构化 ToolError 投影。 |
| Unknown | 参考内部是否兼容历史搜索字段，以及是否存在通用 invalid-args fingerprint、连续 repeated-only batch 熔断或准确阈值。 |

`ToolValidationResult.details/correctionSignature`、`search_text` 的 `maxResults` legacy-only 兼容、
只哈希 Tool 声明安全纠错摘要的 Run-owned validation governance、两批 repeated-only `TOOL_ERROR` 熔断及
stdio/TUI boolean 白名单均为 codej 独立设计，见 ADR-087；不声称为参考内部类型、默认阈值或稳定外部协议。

## 24. S15 Session Task Board 机制观察

2026-08-25 对授权快照中的任务创建、更新、列表、详情、依赖、认领、订阅与终端展示职责做受控窄读。
只记录职责、状态、不变量、失败恢复和验证方法；未复制或翻译函数体、Prompt、注释、错误文案、私有名称、
文件布局、常量、Fixture 或源码字节。

| 分类 | 结论 |
| --- | --- |
| Observed | 任务使用 pending、in-progress、completed 三种主状态；blocked 可由未完成依赖推导，而不是必须成为第四种主状态。 |
| Observed | 紧凑列表与单项详情分离；依赖、owner 与当前状态共同决定任务是否可运行。 |
| Observed | claim 需要在检查阻塞、完成和已有 owner 后原子收敛；删除需要处理其他任务的依赖引用。 |
| Observed | Team/多进程任务共享需要额外所有权、通知、文件变化观察和漏事件恢复；这些职责不同于单 Session 内存状态。 |
| Observed | TUI 对进行中、未阻塞 pending、blocked 和最近 completed 使用稳定优先级与有界展示，UI 不取得任务执行权。 |
| Observed | 紧凑任务列表不依赖全宽边框；进行中项强调，完成项使用删除线/弱化，阻塞项弱化，最近完成优先保留。 |
| Observed | 当前 root 任务的活动文案由加载 spinner 消费；Task List 的进行中项使用语义色实心图标和加粗标题，不重复 root 活动文案。列表内额外 activity 属于具有 owner 的协作执行可观察性，不是本地 `activeForm` 的第二份投影。 |
| Observed | 任务正文按终端显示宽度截断；未完成时持续获取变化，全部完成状态短暂可见后自动退出常驻展示。 |
| Observed | Plan Mode 可调用普通 TaskCreate/TaskUpdate/TaskList/TaskGet；批准工件与 Task Board 是独立对象，执行不会从 Plan Markdown 重新生成另一套 Task identity。 |
| Observed | 模型侧 TaskUpdate 接收 taskId 和可选业务字段；宿主先读取当前任务，再处理状态、owner、依赖与正文变化。 |
| Inferred | codej 的恢复不应把中断中的任务解释为可安全重放副作用；claim 健康可由 Run 终态派生并要求显式处理。 |
| Unknown | 准确发行版本、稳定外部 Task schema、全部 Team/跨进程一致性和公开兼容承诺。 |

单向 canonical `blockedBy`、全图环检查、Board/Task revision、claim epoch、Session-local root-owned board、
资源上限、`TASK_*` 错误、四个 snake_case BUILT_IN Tool schema、精确 Hard Denial 映射、16KiB Get Gate 与
`JsonNull` removal 语义均为 codej 独立设计，见 ADR-088；参考实现不作为 schema、常量、文件格式或测试 Oracle。
2026-08-25 后续窄读补充 Surface 状态/宽度/可见性机制；当时 codej 独立加入的批准步骤解析与 Runtime seeder
随后被真实 Provider identity 分裂故障证伪，现已删除。2026-08-27 的再次验收同时证明：不能把“参考实现未显示应用层 Task Gate”误写成 codej 必须接受未收敛 Task 的最终 prose；codej 独立采用不可伪造的当前 `planId` Task metadata、只观察该 cohort 的 review/reminder/final Gate，以及 Task+Evidence final acceptance，作为防止模型跳过 Task 生命周期且不受同 Session 历史 Task 干扰的确定性产品保证，不声称它们是参考内部机制。基于 Task 中英文标题判断“元任务”的正则不属于授权源码事实，且会误拒绝合法任务，因此不采用。
2026-08-26 针对真实交互失败再次复核相同授权快照，确认 Plan 与普通 Run 使用同一任务工具语义，状态与
`activeForm` 可在同一 Task Update 中出现，且模型不维护 revision/claim。codej 保留 Java 内部 CAS、capability 和
稳定 phase mutation ID 作为独立安全设计，但不暴露为模型参数。旧 30m approved-plan 总 deadline 已被真实流程
证伪并删除；30m 现在只属于 Print/调用方显式 hard deadline 默认值。
2026-08-26 终端交互再复核纠正了第 401 行旧解读：参考 Surface 的本地当前 Task `activeForm` 由加载 spinner 消费，列表主行不再复制同一文案；只有协作 owner 的外部活动才可作为额外列表 activity。该机制事实直接证伪 codej 同时渲染两个 Task Surface 的设计。codej 后续独立把 spinner、subject 与一次 activeForm 统一到唯一 `SessionTaskPanel`，并从模型进度行移除 Board；这是满足“单一 Task Surface”的项目设计，不声称复制参考组件布局。
